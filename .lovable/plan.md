

## Plan: Rename "Blog" to "Health Resource Hub" in Footer

**File: `src/components/layout/Footer.tsx`** (line 25)

Change the blog link text from `t('footer.links.blog')` to the hardcoded string `"Health Resource Hub"`. The link path (`/blog`) stays the same.

