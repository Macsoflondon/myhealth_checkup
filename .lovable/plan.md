
User wants the "Nationwide Coverage" section on `/find-clinic` shrunk to match the compact rhythm applied to `JourneySimplified` and `StartJourneySection`.

Selector points to `#root > main > section:nth-of-type(2)` on the FindClinicPage. Need to inspect that page to find the section.
