Two targeted font-size fixes in HeroMasthead:

1. Revert the "One price you can actually trust" heading back to its original size (`text-[22px]`).
2. Increase the rotating slide-label bubble text (currently `text-xs`) by six standard font sizes.

Both changes are isolated to `src/components/sections/HeroMasthead.tsx`.