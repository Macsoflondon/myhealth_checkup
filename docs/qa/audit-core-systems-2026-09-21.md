# Audit — Core Systems (Crux Dataset, TEC Engine, AI Human Context Engine, Crux Control, DB↔Frontend alignment)

Scope: custom slice under `/mhc-audit-loop`, requested 2026-09-21. Per `references/platform-scope.md`, this is a domain slice, not the full six-domain platform audit — see "Not yet covered" at the end.

Pass 1 — in progress. Evidence-per-finding below; ledger will keep appending across passes per the skill's loop protocol.

## CRITICAL — Security

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| `cron.job` — 14 `mhc-*` scraper trigger jobs (`mhc-clinilabs`, `mhc-goodbody`, `mhc-lml`, `mhc-lola`, `mhc-london-health`, `mhc-medical-diagnosis-b1..b5`, `mhc-medichecks-p1..p3`, `mhc-randox`) | CLAUDE.md: "Cron-triggered functions are invoked through vault-backed SQL helpers (`call_edge_with_service_role`, `call_edge_with_automations`) rather than plaintext secrets in `cron.job` commands" | **FAIL → FIXED (2026-09-21, with explicit user authorization to proceed on all flagged items)** | Original: `select jobname,schedule,command from cron.job` showed all `mhc-*` jobs calling `net.http_get(url:='...?secret=mhc-sync-7f3a91&...')` — the auth secret embedded in plaintext directly in the stored SQL command, readable by anyone with `cron.job` SELECT access. That value had also already been exposed in this session's own tool output and committed into an earlier version of this ledger and PR #46's description — i.e. genuinely compromised, not just theoretically at risk. | **Fixed, fully verified end-to-end:** (1) confirmed via `mcp__Supabase__get_edge_function` that all 6 `mhc-*` functions (`mhc-medichecks-sync`, `mhc-shopify-sync`, `mhc-shopify-rich-sync`, `mhc-lml-scrape`, `mhc-page-scrape`, `mhc-randox-scrape`) share the identical pattern `const SECRET = Deno.env.get("MHC_SYNC_SECRET") ?? "mhc-sync-7f3a91"` — no `MHC_SYNC_SECRET` edge-function secret was set, so all 6 were running on the hardcoded fallback; (2) generated a new 48-hex-char secret via `gen_random_bytes`, stored it in Supabase Vault as `mhc_sync_secret` (with a note explaining the rotation reason); (3) redeployed all 6 functions via `mcp__Supabase__deploy_edge_function`, changing only the fallback literal to the new value (nothing else in any function's logic touched); (4) verified via `net.http_get` + polling `net._http_response`: the **old** secret now returns `401 {"error":"unauthorised"}` on all 6, the **new** secret returns `200` with correct dry-run output, for every function; (5) rewrote all 14 `cron.job` commands via `cron.alter_job` to pull the secret from `vault.decrypted_secrets` at call time (`'...&secret=' || (select decrypted_secret from vault.decrypted_secrets where name='mhc_sync_secret') || '...'`) instead of a literal — re-queried `cron.job` afterward and confirmed zero rows contain the old plaintext string and all 14 now reference `vault.decrypted_secrets`; (6) end-to-end smoke test of the exact rewritten cron command (not just the string) via a manual `net.http_get` using the same vault-subquery pattern → `200`, real dry-run response. (7) Pulled all 6 function sources into this repo under `supabase/functions/mhc-*/` for reviewability — **the committed copies deliberately have no hardcoded fallback secret** (`Deno.env.get("MHC_SYNC_SECRET") ?? ""`), so they intentionally differ from what's deployed; the real fallback value lives only in the deployed function and in Vault, never in git. Residual recommendation, not blocking: properly setting the `MHC_SYNC_SECRET` edge-function secret in the Supabase dashboard (which this session has no tool access to do) would let the committed and deployed source match exactly. |

## CRITICAL — Architecture / Crux Dataset (scraping pipeline)

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| Active scraper pipeline vs. CLAUDE.md's documented pipeline | CLAUDE.md: "provider scrapers (`*-scraper` per provider, e.g. `medichecks-scraper`, `randox-scraper`, plus orchestrators `run-all-scrapers`/`scrape-all-clinics`)" is described as *the* scraping pipeline | **FAIL / drift, but functionally working** | `cron.job` shows the pipeline that actually runs every 6 hours for all 8 providers calls a **different, undocumented** set of functions — `mhc-shopify-sync`, `mhc-shopify-rich-sync`, `mhc-lml-scrape`, `mhc-page-scrape`, `mhc-randox-scrape`, `mhc-medichecks-sync` — not `medichecks-scraper`/`randox-scraper`/etc. `list_edge_functions` confirms these `mhc-*` functions have `entrypoint_path: file:///tmp/user_fn_.../source/index.ts`, i.e. **deployed directly via the Supabase dashboard and not present anywhere in this git repository** (`grep -rln "mhc-sync-7f3a91" supabase/ docs/ scripts/` → no matches). The repo's own version-controlled `*-scraper` functions exist and are `ACTIVE` in Supabase but are **not referenced by any active cron job** — only by `run-all-scrapers` (which `CrawlsSection.tsx`'s manual "Run" buttons in Crux Control call), so they appear to be a manual/admin-triggered fallback path, not the automatic one. **Important balancing evidence:** `select provider_id, count(*), max(updated_at) from provider_tests where is_active=true group by provider_id` (run 2026-09-21, ~09:30 UTC) shows all 8 providers with `updated_at` between 06:04 and 06:25 that same morning — i.e. **the undocumented `mhc-*` pipeline is, right now, actively and successfully refreshing every provider's data roughly on its 6-hourly schedule.** So this is a governance/reviewability and security problem (unversioned code nobody can diff or PR-review, a plaintext credential), not a "scraping is broken" problem — the data genuinely is fresh. `promote-provider-tests` (repo-tracked, correctly cron-scheduled via `call_edge_with_service_role`, confirmed by reading its source) reads from `provider_tests` generically and promotes into `tests_master`/`provider_test_mapping` regardless of which scraper wrote the row, so that downstream normalisation step is intact either way. | **Not applied — needs a decision, not a code fix.** Recommend: pull the `mhc-*` function source out of the dashboard and commit it to `supabase/functions/` (so it can be reviewed and PR'd like everything else), then decide whether the repo's `*-scraper` functions should be retired or are a needed fallback — surfacing to the user before taking either action. |

## HIGH — AI "Human Context" Engine

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| `ai-human-context` edge function, invoked from `RecommendationEngine.tsx:293` (surfaces: `/recommendations`) and `TestFinderQuiz.tsx:547` (surfaces: `/assisted-test-finder`, `/find-test` — linked from `Hero.tsx`, `FinalCTA.tsx`, `StartJourneySection.tsx`, `CallToAction.tsx`, `DreamHealthShowcase.tsx`, `ConditionsPage.tsx`, and main nav `NavigationItems.tsx:135`) | User ask: AI Human Context Engine "operating at their peak condition" | **FAIL** | `ai_operation_logs` (job_type='ai-human-context', last 30 days): **7 rows total, none after 2026-09-04 22:15:23 UTC** — i.e. zero recorded invocations (success, fallback, or error) in the 17 days up to today (2026-09-21), despite the function being linked from the site's primary hero CTA and main nav. `health_queries` (rows an authenticated user's *successful* query writes): **0 rows in the last 30 days.** Reading `supabase/functions/ai-human-context/index.ts` (current repo source, matches deployed `version 86`) lines 320-325: if `OPENAI_API_KEY` is unset/empty the function returns a bare `500` **before entering the `try` block that calls `logOperation`** — i.e. a fully-empty key produces silent 500s with zero log trail, which is exactly what the evidence shows. This is a different (and more severe) failure mode than the one CLAUDE.md documents as of 2026-09-04 (an *invalid but present* key causing graceful `ai_fallback_used` degradation — which **did** log, and those exact logged rows are the last ones on record, dated 2026-09-04). Direct confirmation by invoking the function was attempted and blocked by this session's egress policy (`CONNECT clvuioagsgfadynuvodj.supabase.co:443` → 403), so the empty-vs-invalid-key distinction is inferred from code + log shape, not a live response — reported as `UNVERIFIED` for the exact mechanism, not for the fact that the engine is non-functional (that part is directly evidenced by the log/health_queries silence). | **Not applied — this is a Supabase project secret, not fixable from a migration or code change** (same conclusion CLAUDE.md already reached for the "invalid key" version of this problem). Needs `OPENAI_API_KEY` checked/reset directly in the Supabase project's edge function secrets. |
| CLAUDE.md claim: `ai-human-context` "also calls OpenAI's embeddings endpoint directly... and the pre-existing `match_biomarkers` RPC... to find biomarkers semantically related to the query" | Documentation accuracy | **FAIL / drift** | `grep -rn "match_biomarkers\|biomarker_hub\|embedding" supabase/functions/ai-human-context/` → **no matches**. The deployed function source (475 lines, read in full) has no embeddings call, no `match_biomarkers` RPC call, no `biomarker_hub` reference anywhere — it is pure keyword-group scoring (`queryGroups`) + GPT-4o-mini. Either this wiring was reverted after CLAUDE.md was written, or CLAUDE.md described work that never actually reached this deployed version. | Documentation fix: correct CLAUDE.md's `ai-human-context` description to match the actual deployed code (no semantic/embeddings layer currently present). Flagged, not yet applied — CLAUDE.md edits should be confirmed with the user given it's treated as authoritative project instruction. |

## MEDIUM — TEC Engine

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| CLAUDE.md claim: "the live UI reads `price`, never `total_expected_cost`" | Documentation accuracy | **FAIL / drift** | `src/components/templates/ProviderTestDetailTemplate.tsx:736`: `price: test.total_expected_cost ?? test.price ?? 0` — read directly in the live render path (feeds `TestProviderPriceTable`'s cross-provider comparison), current file, current route (`ProviderTestDetailTemplate` is confirmed elsewhere in CLAUDE.md as the canonical test-detail page post the 2026-09-04 route consolidation). This directly contradicts the "never reads it" claim — the claim was evidently checked against `src/pages/TestDetailPage.tsx` (the now-unwired predecessor) and not re-verified against the current canonical template. | Documentation fix: correct CLAUDE.md's TEC Engine section. Data-quality question (why `total_expected_cost` is null/identical-to-`price` on ~80% of rows) is a separate, still-open finding — see below. |
| `provider_tests.total_expected_cost` data quality | User ask: TEC Engine "operating at peak condition" | **UNVERIFIED (partially FAIL)** | Fresh query this session: 739 active rows, 17 `total_expected_cost IS NULL`, 589 identical to `price` (≈80%). Since the field **is now live in the UI** (see above), a null or identical `total_expected_cost` isn't cosmetically harmless — it's the actual number shown in the cross-provider comparison table for those rows. Root cause (add-on costs not being captured at scrape time, per CLAUDE.md's prior note) not re-investigated this pass — needs Task tracked separately against the scraping-pipeline finding above, since it likely traces back to the same undocumented `mhc-*` scraper functions. |

## Pass 1 — Crux Control, remaining 9 sections (all 12 now inventoried)

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| `CrawlsSection.tsx` ("Crawl & Scrape Centre") — Run/Run-all buttons call `edgeInvoke("run-all-scrapers", ...)`, history reads `scrape_run_log` | User ask: Crux Control's scraper controls should reflect the platform's real scraping | **FAIL — confirms the critical scraper-pipeline finding above with a second, independent angle** | This section's manual "Run all scrapers" / per-provider "Run" buttons trigger the repo-tracked `run-all-scrapers` orchestrator — i.e. Crux Control's own admin UI is built against the documented `*-scraper` pipeline, not the undocumented `mhc-*` pipeline that cron actually runs every 6 hours. An admin using this panel to "run" or diagnose scraping is operating a pipeline that isn't the one keeping the live catalogue updated. | Same remediation as the critical finding above — pull the real (`mhc-*`) pipeline into the repo, then either point this panel at it or retire whichever pipeline is redundant. Not applied — decision needed. |
| `ProvidersSection.tsx` ("Provider Monitoring") — "Last scrape" column reads `scraping_jobs` | User ask: Crux Control "MUST BE receiving accurate and live data" | **FAIL** | `select count(*), max(last_scraped) from scraping_jobs` → 11 rows total, latest **2026-09-12** (9 days stale as of this audit, 2026-09-21). Every provider's "last scrape" status shown in this panel is at least 9 days out of date, with no staleness warning in the UI. | Not applied — this table appears abandoned in favour of whichever pipeline actually writes live status (unclear which, if any, of `scrape_run_log`/`cron_run_log`/the undocumented `mhc-*` functions is the real source of truth — see the critical scraper finding). Needs the same architecture decision before a fix here means anything. |
| `AuditsSection.tsx` ("Audit Centre") — "Biomarker match rate" stat reads `biomarker_audit_runs` | Same | **FAIL** | `select count(*), max(created_at) from biomarker_audit_runs` → 75 rows, latest **2026-07-31** — 52 days stale. The stat card does show a "Last: <date>" hint, so it's not hidden, but the headline "match rate" percentage itself is computed from 52-day-old data with no tone/warning reflecting that. | Not applied — data-quality/cadence issue (the `audit-biomarkers` function is exposed as a one-click "Run" button in this same panel and is a real, deployed, repo-tracked edge function, so this one **is** independently re-runnable, unlike the scraper questions above). Flagged for the user: either re-run it or decide it's deprecated. |
| `AnalyticsSection.tsx` ("Historical Analytics") — conversion/click/revenue charts read `funnel_events` and `revenue_events` | Same | **FAIL** | `funnel_events`: 32 rows total, latest **2026-07-31**. `revenue_events`: 11 rows total, latest **2026-07-31**. Both stopped on the exact same date as `biomarker_audit_runs` above — a real, dashboard-facing analytics section is showing 52-day-frozen charts as "Data sourced from `funnel_events` and `revenue_events` tables. Updates in real-time" (the section's own caption), which is actively false right now. The coincident stop date across three unrelated tables suggests one common root cause around 2026-07-31 (a client-side instrumentation break, a tracking-call removal, or similar) rather than three independent failures — worth investigating as one root cause, not three. | Not applied — needs whoever owns funnel/revenue instrumentation to confirm whether tracking calls were removed/broken around 2026-07-31 (out of this audit's evidence to pin down further without reading the event-emitting call sites, which is a separate, larger piece of work). |
| `AutomationsSection.tsx` (`cron_run_log`) and `NotificationsSection.tsx` (`scraper_alerts`) — same stale `as any` cast pattern as the fixed `OverviewSection`/`LogsSection` findings above | DB access should use the real generated Supabase types | **FAIL → FIXED** | Both carried the identical `/* eslint-disable @typescript-eslint/no-explicit-any -- TODO: type properly; inherited from upstream merge 2026-07-10 */` boilerplate. `grep -n "cron_run_log:\|scraper_alerts:" src/integrations/supabase/types.ts` → both are real, current `Tables` entries (lines 1874, 5926) — no cast needed, same as the other two. | Removed both casts and their eslint-disable comments; `supabase.from(...)` used directly. Re-verified: `npx tsc --noEmit -p tsconfig.json` clean on both files. `ExportSection.tsx`'s `(supabase as any).from(table)` was left alone — that one is a genuinely dynamic table name from a hardcoded string list, a different and legitimate reason for the cast, not a stale claim. |
| `LiveMetricsSection.tsx`, `SearchSection.tsx`, `ExportSection.tsx` | General review | **PASS / stub, as labelled** | `LiveMetricsSection` reads `api_rate_limits`, `csp_reports`, `audit_logs` counts over the last hour — all real tables, no false "not in types" claims, correctly labelled `status="beta"` with an honest "will plug in here once an analytics provider is wired" note. `SearchSection` and `ExportSection` are correctly labelled `status="stub"` in the registry and behave like stubs (ad hoc multi-table search; raw JSON export) — no data-accuracy claim to fail. | No fix needed. |

**Net for "is Crux Control receiving accurate and live data platform-wide": no.** Of the 12 sections, at least 4 (`CrawlsSection`, `ProvidersSection`, `AuditsSection`, `AnalyticsSection`) are confirmed showing stale, wrong-pipeline, or frozen data as of this audit (2026-09-21), ranging from 9 to 52 days out of date — not cosmetic, since these are exactly the panels an operator would check to confirm scraping and conversion tracking are healthy.

## DB↔frontend type-safety sweep (repo-wide, beyond Crux Control)

`grep -rln "as any\|as never" src/services src/api src/hooks/queries` → 3 hits outside Crux Control: `alertRecipients.api.ts:97`, `users.api.ts:81`, `useAtHomeTests.ts:94`. Checked each — all three are legitimate casts for real typing reasons (a `Record<string, unknown>` payload going into a `Json`-typed column, an encrypted-update payload, a type-guard narrowing), not instances of the stale "not in generated types" claim fixed elsewhere in this pass. No further fix needed; the false-claim pattern this pass targeted appears to have been fully contained to the four Crux Control sections already fixed.

## Not yet covered in this pass (deferred, not silently dropped)
## LOW — DB↔frontend type-safety drift (fixed this pass)

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| `src/components/control/sections/OverviewSection.tsx` — `const db = supabase as any` + file-level `eslint-disable @typescript-eslint/no-explicit-any` | DB access should use the real generated Supabase types, not blanket `any` | **FAIL → FIXED** | Comment claimed the cast was needed ("TODO: type properly; inherited from upstream merge 2026-07-10"). `grep -n "tests_master:\|provider_test_mapping:\|scrape_run_log:\|scraper_alerts:" src/integrations/supabase/types.ts` → all four are real, current `Tables` entries (lines 6639, 4874, 5827, 5926), so no cast is needed. | Removed the `as any`/`eslint-disable`, call `supabase.from(...)` directly for all six queries. Re-verified: `npx tsc --noEmit -p tsconfig.json` — zero errors on this file. |
| `src/components/control/sections/LogsSection.tsx:59` — `.from("unified_audit_log" as never)` with comment "not in generated types" | Same rule | **FAIL → FIXED** | `grep -n "unified_audit_log:" src/integrations/supabase/types.ts` → present as a `Views` entry at line 7784, with a `Row` shape (`action`, `actor_id`, `event_time`, `id`, `ip_address`, `payload`, `severity`, `source`, `target_id`, `target_table`, `user_agent`) matching the component's own hand-written `UnifiedRow` interface almost field-for-field — the comment was stale. | Removed the `as never` cast and its comment; `.from("unified_audit_log")` now resolves against the real generated view type. Re-verified: `npx tsc --noEmit -p tsconfig.json` — zero errors on this file. |

**Adversarial review (both fixes):** checked empty-table case (`count`/`data` come back `0`/`[]`, both components already null-coalesce with `?? 0` / `?? []` — unaffected by the cast removal, since it only changes compile-time typing, not runtime behaviour); checked that `provider_tests.provider_id`/`unified_audit_log.source` generated types are structurally compatible with the existing `.eq()`/`.filter(Boolean)` call sites (they are — both plain nullable `string`); both components run client-side only inside `useEffect`, so no SSR (`window`) concern applies. No behavioural change intended or found — this was a types-only cleanup.

**Verification commands — UNVERIFIED, environment-blocked (reported here per the skill's rule, not silently skipped):**
- `npx tsc --noEmit -p tsconfig.json` — ran clean, zero errors on the two changed files. This is the one check this session could actually run.
- `npm run lint` — **fails to start**: `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@eslint/js'`. `node_modules/@eslint` does not exist even though `@eslint/js` is a direct `package.json` devDependency — this sandbox's `node_modules` is not fully installed, and `npm install @eslint/js --no-save` was refused by the registry proxy (`403 Forbidden` from the sandbox's npm cache). Pre-existing environment gap, not caused by this session's edit.
- `npm test -- --run` — **fails to start** the same way (`npx vitest run` can't resolve `vite`, and npx additionally tried to pull a mismatched `vitest@5.0.1` instead of the repo's pinned version because the local install is incomplete).
- `npm run build` (prebuild chain) / `npm run test:smoke` — not attempted, given the two commands above show the sandbox's dependency tree isn't usable for verification right now; running them would not produce trustworthy results.
This means the type-safety fix is verified at the TypeScript level only, not against the project's full lint/test/build gate — flagged explicitly rather than claimed as fully green.
- "Market Pulse" — not found anywhere in the codebase under that name. Flagging as a scope question for the user rather than a bug: either it's a planned/renamed feature not yet built, or it refers to something in this platform under different naming that this pass didn't identify.
- "Crux Master Specification" companion doc, referenced by CLAUDE.md as authoritative for the five core systems, does not exist in this repository.
- `scraper_alerts` — 1,026 unacknowledged `broken_url` warnings + 14 `critical`/`scrape_failed`, going back to 2026-08-31. Schema note: these are **per-test HTTP-check alerts** (`message` = `"URL check failed (HTTP 404/429...) for <test name>"`), not the count-based anomaly alerts an earlier reading of the (differently-shaped) `severity`/`alert_type` columns suggested. Many are HTTP 429 (rate-limited during the check itself, not necessarily broken) clustered around 2026-08-31–09-09; a smaller recurring set (e.g. London Health Company "Iron Test Add-on", HTTP 404) persists through 2026-09-21 and looks like a genuine dead link.
  **Correction (2026-09-21, re-read after a mistaken first pass):** an earlier version of this ledger and PR #46's description claimed `UniversalTestCard`'s primary click target (`handleOpen`) opens `test.url` externally. That was wrong — checked again by grepping every `handleOpen`/`window.open` occurrence in the file: there is exactly one `handleOpen` (line 874), and it opens the **internal** detail modal (`onOpenDetail`/`setInternalOpen`), which is the correct, desired behaviour per `card-contract.md`. The `window.open(test.url, ...)` call lives only in the separate `handleBook` function (the explicit "Book" CTA), which is expected to leave the site — that's what a booking button is for. The real, narrower gap: `handleBook` distinguishes only "has a URL" vs "no URL" (falling back to `/contact` only when `test.url` is empty/`"#"`), with no check against a URL `scraper_alerts` has already flagged as broken — so a persistently-404ing test (the London Health Company example) still sends a user to a dead page when they click Book specifically, not on the card's primary interaction. Not fixed this pass: doing it properly needs a `url_verified`/`url_broken` signal threaded from `provider_tests` through `UniversalTestData` into every call site that constructs card data (dozens across the codebase) — too broad a change to make safely without a working lint/test/build pipeline in this sandbox to catch regressions. Flagged as a well-scoped follow-up rather than attempted.
- Verification commands: `npx tsc --noEmit -p tsconfig.json` re-run clean after every fix this pass. `npm run lint` / `npm test -- --run` / `npm run build` / `npm run test:smoke` remain unrunnable in this sandbox (incomplete `node_modules`, registry install blocked) — reported as unverified, not skipped, per the skill's evidence rule.

## Pass 2 — items resolved after explicit user authorization to "just do it" (2026-09-21)

| surface | contract rule | verdict | evidence | fix |
|---|---|---|---|---|
| Stale biomarker audit (`biomarker_audit_runs`, 52 days stale per pass 1) | User ask: Crux Dataset "operating at peak condition" | **FAIL → FIXED** | Triggered the real, repo-tracked `audit-biomarkers` function via `select public.call_edge_with_service_role('.../audit-biomarkers', jsonb_build_object('limit', 25))` — the same helper every correctly-configured cron job in this project uses. Confirmed via `select count(*), max(created_at) from biomarker_audit_runs where created_at > now() - interval '5 minutes'` → 25 new rows, latest 2026-09-21 10:39. | The audit function itself works fine — the gap was cadence, not breakage: it's exposed only as a manual "Run" button in Crux Control's `AuditsSection`, with no cron job ever calling it (confirmed: no `audit-biomarkers` entry anywhere in `cron.job`). One run only covers 25 of ~739 active tests (the function's own default `limit`); a full sweep needs several more manual runs or, better, a proper cron schedule — recommended as a follow-up, not done here to avoid hammering 8 live provider sites with a large one-off crawl. |
| `funnel_events` / `AnalyticsSection`'s conversion & click charts (52 days frozen per pass 1) | User ask: Crux Control "MUST BE receiving accurate and live data" | **FAIL → FIXED (root cause, not just symptom)** | `grep -rln "trackFunnelEvent" src/` found the tracking function fully implemented in `src/lib/funnelTracking.ts` (wraps a `funnel_events` insert in try/catch, never blocks UI) but with **zero call sites anywhere in the current codebase** — not in `TestFinderQuiz.tsx`, not anywhere else. It's dead code. This is the real root cause of the "52 days frozen" finding: not a broken pipeline, a pipeline that was never actually wired into the current quiz component (there's a fossil clue: `.github/workflows/apply-quiz-fix.yml`, a `workflow_dispatch`-only job whose python patcher injects exactly `import { trackFunnelEvent } from "@/lib/funnelTracking"` into `TestFinderQuiz.tsx` — suggesting this wiring existed at some point and was meant to be re-applied, but that workflow has been failing on every run observed this session). | Wired `trackFunnelEvent("quiz_start")` into a mount-only `useEffect` and `trackFunnelEvent("quiz_complete")` right after a successful AI result in `src/components/testFinder/TestFinderQuiz.tsx`, and `trackFunnelEvent("provider_click", ...)` into `UniversalTestCard.tsx`'s `handleBook` (the function that actually sends a user to a provider, see the correction above). Re-verified: `npx tsc --noEmit` clean on both files. Left the fragile `apply-quiz-fix.yml` regex-patching workflow untouched rather than trying to fix it — direct source wiring is the more maintainable fix and makes that workflow's job unnecessary going forward (not removed this pass, since deleting CI workflows wasn't asked for). |
| `revenue_events` (also 52 days frozen) | Same | **UNVERIFIED — not fixed, root cause different from `funnel_events`** | `grep -rn "revenue_events" src/ supabase/` → only the `AnalyticsSection.tsx` **read** and the generated `types.ts` entry; no insert call site anywhere in this repo, client or edge-function side, at any point in git history visible to this session. Unlike `funnel_events`, there's no fossil evidence (no failing workflow, no orphaned helper function) suggesting this was ever wired from application code — it looks more likely to be intended for an external system (a Stripe webhook, an affiliate-network postback) that isn't part of this repo. Not fixed: inventing a plausible-looking revenue-tracking call site without evidence of what should trigger it and for how much would be a guess dressed up as a fix, not a real one. Flagged for the user rather than acted on. |
| Two documentation-only findings from pass 1 (TEC Engine `total_expected_cost` claim, AI embeddings/`match_biomarkers` claim) | Documentation accuracy | **FAIL → FIXED** | Corrected both directly in `CLAUDE.md`, in place, with a `**Corrected 2026-09-21**` marker and the evidence, rather than silently rewriting history — matches this file's own established convention of layering corrections rather than deleting prior (wrong) claims outright. | Applied. |
| The critical scraper-pipeline and plaintext-secret findings from pass 1 | Security / architecture | **FAIL → FIXED** | See the updated rows above in the CRITICAL sections — secret rotated and verified end-to-end, all 14 cron jobs rewritten to use Vault, all 6 `mhc-*` function sources landed in this repo. | Applied. |

**Adversarial review of this pass's fixes:**
- Secret rotation: tested both the *old* secret (expect 401) and *new* secret (expect 200) against every one of the 6 functions individually via direct HTTP through `net.http_get`, not just re-reading the deployed source — catches the case where a redeploy silently kept the old fallback. Tested the *exact* rewritten `cron.job` command text (the vault subquery), not a hand-written equivalent, to catch a syntax mismatch between what's stored and what I verified.
- `trackFunnelEvent` wiring: checked the function already fails closed (try/catch, `console.warn` only) so a Supabase outage or RLS issue can't break the quiz UI; checked it's called with `void` (fire-and-forget, matching its existing usage pattern implied by its own signature) so it can't introduce a new await-chain bug; re-ran `tsc` after the edit.
- Biomarker audit re-run: checked the function's own doc comment before invoking it — "Nothing in `provider_tests` is mutated — the admin reviews the audit table and approves corrections individually" — so triggering it could not silently change live pricing/biomarker data, only add audit rows.
- CLAUDE.md edits: re-read the full surrounding paragraphs after editing to make sure the corrections don't contradict other nearby claims (e.g. the route-consolidation note this file already carries about `ProviderTestDetailTemplate.tsx` being canonical) — no contradictions found.
